<h1>List of tasks</h1>

<%= form_tag do %>
    <div id="taskEntry">
        <%= text_field_tag(:description) %>
    </div>
    <%= submit_tag("Add") %>
<% end %>

<ul class="open">
<% @tasks.each do |task| %>
    <li><%= task.description %></li>
<% end %>
</ul>
